version: '3.4'

services:
  catalog:
    image: ${DOCKER_REGISTRY-}catalog
    build:
      context: .
      dockerfile: Catalog/Dockerfile

  wineweb:
    image: ${DOCKER_REGISTRY-}wineweb
    build:
      context: .
      dockerfile: WineWeb/Dockerfile



